# Mythos runs CPU-only. On a machine without a GPU, install the CPU build of
# torch first to avoid pulling CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
psutil>=5.9          # used for auto-selecting a preset from available RAM
httpx>=0.27          # for the FastAPI test client / smoke tests

# optional, only for the test suite
pytest>=8.0
